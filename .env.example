# VisionAI Eye Hospital - Environment Configuration
# Copy this file to .env and fill in your credentials

# Flask Configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# Hugging Face (optional - only needed if model download is rate-limited)
# HF_TOKEN=your-huggingface-token-here
# Note: AI model runs locally. No API token needed for inference.

# Google OAuth (for "Sign in with Google")
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google Sheets (for user registration logging)
# Create a service account at: https://console.cloud.google.com/iam-admin/serviceaccounts
# Download the JSON key file and save as credentials.json in the project root
GOOGLE_SHEETS_CREDENTIALS=credentials.json

# Supabase Database (replaces SQLite)
# Get these from: https://supabase.com → Project Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

# CORS Allowed Origins (comma-separated list of allowed origins)
# Defaults to http://localhost:5000,http://127.0.0.1:5000 if not set
ALLOWED_ORIGINS=http://localhost:5000,https://yourdomain.com

# URL Scheme for OAuth redirect URIs (set to 'http' for local dev, 'https' for production)
# Defaults to 'https' if not set
# PREFERRED_URL_SCHEME=http
